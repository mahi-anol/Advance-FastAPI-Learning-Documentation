# Start PostgreSQL in the background
docker compose up -d

# Check if container is running and healthy
docker compose ps

# View logs if needed
docker compose logs -f db

docker compose exec db psql -U postgres -d test_db -c "SELECT version();"


pip install -r requirements.txt


alembic init alembic

alembic revision --autogenerate -m "Create user table"

# Apply all pending migrations
alembic upgrade head


docker compose exec db psql -U postgres -d test_db -c "\dt"
docker compose exec db psql -U postgres -d test_db -c "\d user"


# Start the API server
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000


# test

curl -X POST "http://localhost:8000
/users"   -H "Content-Type: application/json"   -d '{"email": "bob@example.com", "username": "bob"}'

curl -X GET "http://localhost:8000/users"

curl -X GET "http://localhost:8000/users/3"


curl -X PUT "http://localhost:8000/users/3" \
  -H "Content-Type: application/json" \
  -d '{"email": "bob.smith@example.com"}'


curl -X DELETE "http://localhost:8000/users/3"


docker compose exec db psql -U postgres -d test_db


# List all users
SELECT * FROM users;

# Count users
SELECT COUNT(*) FROM users;

# Exit psql
\q